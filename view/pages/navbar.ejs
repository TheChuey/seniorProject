<section id="logInVerfication">
    <% if (login === true) { %>
    <div class="title">
        <h1>Log In</h1>
        <a class="navbar-brand text-white" href=" #"></a>
    </div>
    <%} else { %>
    <p> Not Allowed </p>
    <div class="logOFF">
        <h1>LogOut</h1>
        <a class="navbar-brand text-white" href=" #"></a>
    </div>
    <% } %>
</section>

<nav class="navbar navbar-expand-lg  bg-dark navbar-dark">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a id="home" onclick="go(`home`)" class="nav-link text-white" href="#"> Home </a>
            </li>
            <li class="nav-item">
                <a id="post" onclick="go(`post`)" class="nav-link text-white" href="#">Post</a>
            </li>
            <!-- <li class="nav-item">
                <a id="post" onclick="go(`postView`)" class="nav-link text-white" href="#">PostView</a>
            </li>
            <li class="nav-item">
                <a id="post" onclick="go(`viewAllpost`)" class="nav-link text-white" href="#">View all Post </a>
            </li> -->
            <li class="nav-item">
                <a class="nav-link text-white" href="/exit">Logout</a>
            </li>
            </li>
        </ul>
    </div>

    <script>
        function go(endPoint) {
            console.log(endPoint);
            // fetch(`/${endPoint}`).then(function (response) {
            //     console.log(response.status);
            //     console.log(response.url);
            //     document.getElementById("2").setAttribute("src", `${response.url}`);
            // });

            if (endPoint === "home") {
                fetch(`/home`).then((response) => {
                    console.log("home", response);
                    document.getElementById("1").setAttribute("src", `${response.url}`);
                    let a = document.getElementsByTagName("h1");
                    console.log(a);
                    a[1].innerText = "home";
                }).catch()
            } else if (endPoint === "post") {
                fetch(`/post`).then((response) => {

                    console.log("post", response);
                    document.getElementById("1").setAttribute("src", `${response.url}`);
                    let a = document.getElementsByTagName("h1");
                    console.log(a);
                    a[1].innerText = "Post";
                }).catch()
            } else {
                fetch(`/exit`).then().catch()
            }
        }
    </script>
</nav>